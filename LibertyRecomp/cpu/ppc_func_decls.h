#pragma once
// Shim — previously held a 37K-line duplicate of rexglue's
// gta4-recomp/generated/gta4_init.h. Now forwards directly so we only
// have one source of PPC_EXTERN_IMPORT(sub_*) declarations.
#include "../../glue/gta4-recomp/generated/gta4_init.h"
