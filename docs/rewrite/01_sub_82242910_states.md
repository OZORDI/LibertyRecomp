# sub_82242910 — Scene Creation State Machine

**Source**: `glue/rexglue-sdk-main/gta4-recomp/generated/gta4_recomp.6.cpp` lines 84816-86159

## Key Addresses

| Symbol | Address | Description |
|--------|---------|-------------|
| STATE_VAR | `0x82BF9848` | Current state (u32), offset -26552 from r26 (0x82C00000) |
| SCENE_MODE | `0x82BF9844` | Scene mode selector (u32), offset -26556 from r26 |
| ERROR_CODE | `0x82A9546C` | Error code output (u32), offset +21612 from 0x82A90000 |
| ONLINE_FLAG | `0x82A95466` | Online/network flag (u8), offset +21606 from 0x82A90000 |
| RESTART_FLAG | `0x82BF981E` | Restart-pending flag (u8), offset -26594 from r26 |
| FLAG_26593 | `0x82BF981F` | Secondary flag (u8), offset -26593 from r26 |
| PENDING_VAL | `0x82BF99CC` | Pending value (u32), offset -26164 from r26 |
| SCENE_INFO_PTR | `0x82BF3D90` | Scene info pointer (u32), offset +15760 from 0x82BF0000 |
| SCENE_STRUCT | `0x82BF3A60` | Scene struct base (r28), offset +14944 from 0x82BF0000 |
| STRUCT_14968 | `0x82BF3A78` | Scene struct at +14968 from 0x82BF0000 |
| BYTE_14966 | `0x82BF3A76` | Byte at +14966 from 0x82BF0000 |
| ADDR_15764 | `0x82BF3D94` | Addr at +15764 from 0x82BF0000 |
| ADDR_15768 | `0x82BF3D98` | Addr at +15768 from 0x82BF0000 |
| ADDR_15578 | `0x82BF3CDA` | Byte at +15578 from 0x82BF0000 |
| STATE_SUBSTRUCT | `0x82BF99C8` | Sub-struct (r29), offset -26168 from r26 |
| SCENE_NAME | `0x83192C50` | Scene name string (r31), lis(-31975)+11344 |

## Register Aliases (set once at function entry)

- **r26** = `0x82C00000` (base for state/mode/flags)
- **r27** = `0x82C00000` (same as r26, used for PENDING_VAL)
- **r28** = `0x82BF3A60` (SCENE_STRUCT)
- **r29** = `0x82BF99C8` (STATE_SUBSTRUCT)
- **r30** = `0x82BF0000` (base for scene info and byte fields)
- **r31** = `0x83192C50` (SCENE_NAME)

## Return Value Convention

- **r3 = 0**: "Finished" / terminal state (success or abort)
- **r3 = 1**: "In progress" / continue calling
- **r3 = 2**: "Error" (ERROR_CODE has been written)

## Guard: state > 14

If STATE_VAR > 14, jumps directly to `loc_82243250` which returns r3=1 (in progress, no-op).

---

## State 0 — Initialize / Check Ready

**Label**: `loc_822429A0`

**Calls**:
- `sub_8223DAA0()` — readiness check (returns bool as u8)

**Logic**:
1. Call `sub_8223DAA0()`.
2. If returns **nonzero** (ready):
   - r3 = SCENE_NAME (`0x83192C50`)
   - r4 = `[SCENE_INFO_PTR]` (load from `0x82BF3D90`)
   - Set STATE_VAR = **4** (skip to loading)
   - Call `sub_8284B4B0(r3, r4)` — likely "set scene name"
   - Return **r3 = 1**
3. If returns **zero** (not ready):
   - Set STATE_VAR = **1**
   - Return **r3 = 1**

**Transitions**:
- Ready -> state 4
- Not ready -> state 1

---

## State 1 — Wait for Network + Content Check

**Label**: `loc_822429EC`

**Calls**:
- `sub_826CBA70()` — network/session check (returns bool u8)
- `sub_8223DAA0()` — readiness check
- `sub_8223F9F0(r3=0, r4=0, r5=&local_80)` — content enumeration check
- `sub_826CB6B8()` — network cleanup/teardown

**Logic**:
1. Call `sub_826CBA70()`.
   - If **nonzero** (network active): call `sub_826CB6B8()`, then set STATE_VAR = **3**, return r3=1.
2. Call `sub_8223DAA0()`.
   - If **nonzero** (ready): set STATE_VAR = **3**, return r3=1.
3. Call `sub_8223F9F0(0, 0, &local_80)` — check content for mode 0.
   - If returns **zero** (failed): goto `loc_82243250`, return r3=1 (wait).
   - If `local_80 != 0` (content found): goto `loc_82242B08` -> set STATE_VAR = **2**, return r3=1.
   - If `local_80 == 0`: set STATE_VAR = **3**, return r3=1.

**Transitions**:
- Network active OR readiness -> state 3
- Content found -> state 2 (but note: code path from `local_80==0` also goes to state 3)
- Content check pending -> wait (return 1)

---

## State 2 — Alternate Content Check Path

**Label**: `loc_82242A4C`

**Calls**:
- `sub_8223CB60()` — returns a bool (saved in r31)
- `sub_826CBA70()` — network check
- `sub_8223DAA0()` — readiness check
- `sub_8223F9F0(r3=1, r4=0, r5=&local_80)` — content enumeration (mode 1)
- `sub_826CB6B8()` — network teardown

**Logic**:
1. Call `sub_8223CB60()`, save result in r31.
2. Call `sub_826CBA70()`. If nonzero -> call `sub_826CB6B8()`, set STATE_VAR = **3**, return r3=1.
3. Call `sub_8223DAA0()`. If nonzero -> set STATE_VAR = **3**, return r3=1.
4. If r31 (sub_8223CB60 result) is nonzero:
   - Call `sub_8223F9F0(1, 0, &local_80)`. If returns zero -> wait (return 1).
5. Check `local_80`:
   - If `local_80 != 0` AND r31 != 0: call `sub_826CB6B8()`, set STATE_VAR = **3**, return r3=1.
   - Otherwise: fall through to `loc_82242AB8`.
6. At `loc_82242AB8`: write ERROR_CODE = **6**, return **r3 = 2** (error).

**Transitions**:
- Network/ready/content -> state 3
- Failure -> ERROR 6, return 2

**Errors**:
- Error code **6** at `0x82A9546C`

---

## State 3 — Network + Ready Check (Pre-Load)

**Label**: `loc_82242AD0`

**Calls**:
- `sub_826CBA70()` — network check
- `sub_8223DAA0()` — readiness check
- `sub_82240AB0()` — pre-load setup

**Logic**:
1. Call `sub_826CBA70()`. If nonzero -> goto `loc_82243250`, return r3=1 (wait for network to settle).
2. Call `sub_8223DAA0()`.
   - If **zero** (not ready): set STATE_VAR = **2**, return r3=1 (go back to state 2).
   - If **nonzero** (ready): call `sub_82240AB0()`, set STATE_VAR = **4**, return r3=1.

**Transitions**:
- Network busy -> wait
- Not ready -> state 2
- Ready -> state 4 (after pre-load setup)

---

## State 4 — Scene Load Setup (Complex)

**Label**: `loc_82242B1C`

**Calls**:
- `sub_8223DB20()` — error/abort check (returns bool u8; nonzero = abort)
- `sub_82240B08()` — secondary validation
- `sub_8223F308(r3=1, r4=STRUCT_14968)` — structure init

**Logic**:
1. Call `sub_8223DB20()`. If **nonzero** (abort): write ERROR_CODE = **33**, return **r3 = 2**.
2. Initialize: PENDING_VAL = 0.
3. Read SCENE_MODE from `0x82BF9844`. If (SCENE_MODE - 3) <= 1 (i.e., mode 3 or 4): set r10=1.
4. If r10 is nonzero (mode 3 or 4):
   - Call `sub_8223F308(1, STRUCT_14968)` — init struct.
   - Copy `[STRUCT_14968 + 4]` to PENDING_VAL.
5. Read RESTART_FLAG (`0x82BF981E`).
   - If **nonzero**: set STATE_VAR = **5**, return r3=1.
6. Call `sub_82240B08()`.
   - If **nonzero**: set STATE_VAR = **9**, return r3=1.
7. **Inner switch on SCENE_MODE** (0-4):
   - **Mode 0, 1**: r11 = 0 (no error flag).
   - **Mode 2**: r11 = 1 (error flag).
   - **Mode 3**: Check FLAG_26593 (`0x82BF981F`). If nonzero, check ONLINE_FLAG. If ONLINE_FLAG == 0, set RESTART_FLAG = 1. r11 = 0. If FLAG_26593 == 0, r11 = 1.
   - **Mode 4**: Check ONLINE_FLAG. If == 0, set RESTART_FLAG = 1, r11 = 0. If != 0, r11 = 1.
   - **Mode > 4**: r11 = 1.
8. If r11 (error flag) is nonzero: write ERROR_CODE = **34**, return **r3 = 2**.
9. Otherwise: set STATE_VAR = **5**, return r3=1.

**Transitions**:
- Abort -> ERROR 33, return 2
- Mode 3/4 with restart -> state 5
- Restart flag set -> state 5
- sub_82240B08 nonzero -> state 9
- Inner switch error -> ERROR 34, return 2
- Success -> state 5

**Errors**:
- Error code **33** (abort detected)
- Error code **34** (invalid scene mode for current network state)

---

## State 5 — Content Enumeration / Load Decision

**Label**: `loc_82242C88`

**Calls**:
- `sub_8223DB20()` — abort check
- `sub_8223F9F0(r3=2, r4=0, r5=&local_80)` — content enumeration (mode 2)

**Logic**:
1. Call `sub_8223DB20()`. If nonzero -> ERROR 33, return 2 (via `loc_82242B2C`).
2. Read RESTART_FLAG.
   - If **nonzero**: set local_80=1, clear RESTART_FLAG=0, set r31=1.
   - If **zero**: call `sub_8223F9F0(2, 0, &local_80)`. If returns nonzero -> r31=1. If zero -> r31 stays 0.
3. If r31 == 0: goto `loc_82243250`, return r3=1 (wait).
4. Clear BYTE_14966 (`0x82BF3A76`) = 0.
5. Compute next state from local_80:
   - `local_80 == 0` (no content): next_state = **7**
   - `local_80 != 0` (has content): next_state = **6**
6. Set STATE_VAR = computed value, return r3=1.

**Transitions**:
- Abort -> ERROR 33
- Content available -> state 6
- No content -> state 7
- Waiting -> return 1

---

## State 6 — Scene Creation with Content (Network Session)

**Label**: `loc_82242D18`

**Calls**:
- `sub_8223DB20()` — abort check
- `sub_826CBA70()` — network check
- `sub_8284AAE0(r3=SCENE_NAME, r4=[SCENE_INFO_PTR], r5=1, r6=PENDING_VAL, r7=1, r8=ONLINE_FLAG)` — create scene with content

**Logic**:
1. Call `sub_8223DB20()`. If nonzero -> ERROR 33.
2. Call `sub_826CBA70()`. If nonzero -> wait (return 1).
3. Load SCENE_NAME, SCENE_INFO_PTR, PENDING_VAL, ONLINE_FLAG.
4. Call `sub_8284AAE0(SCENE_NAME, [SCENE_INFO_PTR], 1, PENDING_VAL, 1, ONLINE_FLAG)`.
   - If returns **zero** (failed): write ERROR_CODE = **7**, return **r3 = 2**.
   - If returns **nonzero** (success): set STATE_VAR = **8**, return r3=1.

**Transitions**:
- Abort -> ERROR 33
- Network busy -> wait
- Creation failed -> ERROR 7, return 2
- Success -> state 8

**Errors**:
- Error code **7** (scene creation failed)

---

## State 7 — Scene Creation without Content (Retry Path)

**Label**: `loc_82242D9C`

**Calls**:
- `sub_8223DB20()` — abort check
- `sub_8223CB60()` — check for additional content
- `sub_8223F9F0(r3=3, r4=0, r5=&local_80)` — content enumeration (mode 3)

**Logic**:
1. Call `sub_8223DB20()`. If nonzero -> ERROR 33.
2. Call `sub_8223CB60()`, save result as r31 (bool).
3. If r31 nonzero:
   - Call `sub_8223F9F0(3, 0, &local_80)`. If returns zero -> wait (return 1).
4. Check local_80:
   - If `local_80 == 0`: goto `loc_82242AB8` -> ERROR 6, return 2.
   - If `local_80 != 0` AND r31 != 0: set STATE_VAR = **6**, return r3=1.
   - If `local_80 != 0` AND r31 == 0: goto `loc_82242AB8` -> ERROR 6, return 2.

**Transitions**:
- Abort -> ERROR 33
- Content found + sub_8223CB60 true -> state 6
- No content or sub_8223CB60 false -> ERROR 6, return 2
- Waiting -> return 1

**Errors**:
- Error code **6** (content not available)

---

## State 8 — Monitor Scene Creation Progress

**Label**: `loc_82242E08`

**Calls**:
- `sub_8284AB10(r3=SCENE_NAME, r4=[SCENE_INFO_PTR])` — check scene creation started
- `sub_8284AB70(r3=SCENE_NAME, r4=[SCENE_INFO_PTR])` — advance scene creation
- `sub_8223DB20()` — abort check
- `sub_8284B490(r3=SCENE_NAME, r4=[SCENE_INFO_PTR])` — get creation status
- `sub_8284B430(r3=SCENE_NAME, r4=[SCENE_INFO_PTR])` — get creation result
- `sub_82240B08()` — secondary validation

**Logic**:
1. Call `sub_8284AB10()`. If returns zero -> wait (return 1).
2. Call `sub_8284AB70()` — advance.
3. Call `sub_8223DB20()`. If nonzero -> ERROR 33.
4. Call `sub_8284B490()`:
   - If returns **1** (in progress):
     - Call `sub_8284B430()`:
       - If returns **2**: set STATE_VAR = **7**, return r3=1 (retry without content).
       - Otherwise: write ERROR_CODE = **8**, return **r3 = 2**.
   - If returns **not 1** (creation not in specific state):
     - Call `sub_8284B430()`:
       - If returns **0**: goto `loc_82242EC0`.
       - Otherwise: write ERROR_CODE = **9**, return **r3 = 2**.
5. At `loc_82242EC0`: call `sub_82240B08()`.
   - If returns **nonzero** (truthy): STATE_VAR = **0** (restart from beginning).
   - If returns **zero** (falsy): STATE_VAR = **9**.
   - Return r3=1.

**Transitions**:
- Waiting for start -> return 1
- Abort -> ERROR 33
- Status=1, result=2 -> state 7 (retry)
- Status=1, result!=2 -> ERROR 8
- Status!=1, result=0, validation true -> state 0 (restart)
- Status!=1, result=0, validation false -> state 9
- Status!=1, result!=0 -> ERROR 9

**Errors**:
- Error code **8** (scene creation error during progress)
- Error code **9** (scene creation error on completion)

---

## State 9 — Begin Scene Loading

**Label**: `loc_82242EE4`

**Calls**:
- `sub_8223DB20()` — abort check
- `sub_82240B78()` — load validation check
- `sub_8223D2F0()` — pre-load preparation
- `sub_8284ABA0(r3=SCENE_NAME, r4=[SCENE_INFO_PTR], r5=1, r6=ADDR_15768, r7=75)` — begin scene load

**Logic**:
1. Call `sub_8223DB20()`. If nonzero -> ERROR 33.
2. Call `sub_82240B78()`. If nonzero -> ERROR 34 (via `loc_82242C48`).
3. Call `sub_8223D2F0()` — pre-load prep.
4. Call `sub_8284ABA0(SCENE_NAME, [SCENE_INFO_PTR], 1, 0x82BF3D98, 75)`.
   - If returns **zero**: write ERROR_CODE = **14**, return **r3 = 2**.
   - If returns **nonzero**: set STATE_VAR = **10**, fall through.

**Transitions**:
- Abort -> ERROR 33
- Load validation fail -> ERROR 34
- Load start failed -> ERROR 14, return 2
- Success -> state 10

**Errors**:
- Error code **14** (scene load initiation failed)
- Error code **34** (load validation failed, shared with state 4)

---

## State 10 — Monitor Scene Load Progress

**Label**: `loc_82242F50`

**Calls**:
- `sub_8284ABD0(r3=SCENE_NAME, r4=[SCENE_INFO_PTR], r5=ADDR_15764)` — poll load status
- `sub_8284ABF8(r3=SCENE_NAME, r4=[SCENE_INFO_PTR])` — advance load
- `sub_8223DB20()` — abort check
- `sub_82240B78()` — load validation
- `sub_8284B490(r3=SCENE_NAME, r4=[SCENE_INFO_PTR])` — get load status
- `sub_8284B430(r3=SCENE_NAME, r4=[SCENE_INFO_PTR])` — get load result (called up to 2x)

**Logic**:
1. Set r29 = 0 (success flag).
2. Call `sub_8284ABD0()`. If returns zero -> wait (return 1).
3. Call `sub_8284ABF8()` — advance.
4. Call `sub_8223DB20()`. If nonzero -> ERROR 33.
5. Call `sub_82240B78()`. If nonzero -> ERROR 34.
6. Call `sub_8284B490()`:
   - If returns **1**: write ERROR_CODE = **15**, return **r3 = 2**.
   - If returns **not 1**:
     - Call `sub_8284B430()`. If returns **0**: goto step 8. If not 0:
       - Call `sub_8284B430()` again. If returns **5**: goto step 8.
       - Otherwise: write ERROR_CODE = **16**, return **r3 = 2**.
7. (not reached if branched)
8. Set r29 = 1 (success).
9. If r29 == 0: wait (return 1).
10. Read SCENE_MODE:
    - If mode is 0, 1, 3, or 4: set STATE_VAR = **11**, return r3=1.
    - Otherwise: goto `loc_82243178` -> call `sub_8223CAD8()`, return **r3 = 0** (finished/abort).

**Transitions**:
- Waiting for load -> return 1
- Abort -> ERROR 33
- Validation fail -> ERROR 34
- Load status=1 -> ERROR 15
- Load result != 0 and != 5 -> ERROR 16
- Load complete, mode 0/1/3/4 -> state 11
- Load complete, other mode -> call sub_8223CAD8(), return 0 (finished)

**Errors**:
- Error code **15** (unexpected load status)
- Error code **16** (unexpected load result)

---

## State 11 — Post-Load Processing

**Label**: `loc_82243058`

**Calls**:
- `sub_8223DB20()` — abort check
- `sub_82240B78()` — validation
- `sub_8223D400()` — post-load processing
- `sub_8223CAD8()` — cleanup/abort

**Logic**:
1. Call `sub_8223DB20()`. If nonzero -> ERROR 33.
2. Call `sub_82240B78()`. If nonzero -> ERROR 34.
3. Call `sub_8223D400()`.
   - If returns **zero**: goto `loc_82242AC4`, return **r3 = 2** (no error code written here; reuses state 2's exit with r3=2).
4. Read SCENE_MODE:
   - If mode 3 or 4: set STATE_VAR = **12**, return r3=1.
   - Otherwise: goto `loc_82243178` -> call `sub_8223CAD8()`, return **r3 = 0** (finished).

**Transitions**:
- Abort -> ERROR 33
- Validation fail -> ERROR 34
- Post-load fails -> return 2
- Mode 3/4 -> state 12
- Other modes -> call sub_8223CAD8(), return 0

---

## State 12 — Multiplayer Scene Setup

**Label**: `loc_822430B4`

**Calls**:
- `sub_822417B0(r3=0, r4=1, r5=SCENE_STRUCT, r6=PENDING_VAL, r7=STATE_SUBSTRUCT, r8=&local_84, r9=&local_88)` — multiplayer scene setup

**Reads**: PENDING_VAL (`0x82BF99CC`)
**Writes**: local_84=0, local_88=0 (on stack)

**Logic**:
1. Initialize local_84=0, local_88=0.
2. Call `sub_822417B0(0, 1, SCENE_STRUCT, PENDING_VAL, STATE_SUBSTRUCT, &local_84, &local_88)`.
   - If returns **2**: goto `loc_82242AC4`, return **r3 = 2**.
   - Otherwise: set STATE_VAR = **14**, fall through to state 14.

**Transitions**:
- sub_822417B0 returns 2 -> return 2
- Otherwise -> state 14

---

## State 14 — Multiplayer Scene Finalization Loop

**Label**: `loc_822430F0`

**Calls**:
- `sub_822417B0(r3=0, r4=0, r5=SCENE_STRUCT, r6=PENDING_VAL, r7=STATE_SUBSTRUCT, r8=&local_88, r9=&local_84)` — continue multiplayer setup (note: r8/r9 args swapped vs state 12)
- `sub_8223F790()` — network session finalization
- `sub_8223CAD8()` — cleanup
- `sub_8223DB90()` — final cleanup

**Reads**: PENDING_VAL, STATE_SUBSTRUCT, ONLINE_FLAG, FLAG_26593, SCENE_MODE

**Logic**:
1. Initialize local_88=0, local_84=0.
2. Call `sub_822417B0(0, 0, SCENE_STRUCT, PENDING_VAL, STATE_SUBSTRUCT, &local_88, &local_84)`.
   - If returns **2**: goto `loc_82242AC4`, return **r3 = 2**.
   - If returns **non-zero and non-0**: wait (return 1).
   - If returns **0**: continue.
3. Read `[STATE_SUBSTRUCT]` (r29 dereference, `0x82BF99C8`):
   - If **negative** (< 0): set STATE_VAR = **13**, return r3=1.
4. Check ONLINE_FLAG:
   - If **nonzero**: goto step 5a.
   - If **zero**: check FLAG_26593.
     - If FLAG_26593 == 0: goto `loc_82242AC4`, return **r3 = 2** (done).
     - If FLAG_26593 != 0: check SCENE_MODE.
       - If mode == 3: call `sub_8223F790()`. If returns nonzero -> goto step 6. If zero -> call `sub_8223CAD8()`, goto step 6.
       - If mode != 3: call `sub_8223CAD8()`, goto step 6.
5. (ONLINE_FLAG nonzero path):
   - Check SCENE_MODE:
     - If mode == 3 AND FLAG_26593 != 0: call `sub_8223F790()`. If nonzero -> goto step 6. If zero -> call `sub_8223CAD8()`.
     - Otherwise: call `sub_8223CAD8()`.
6. Return **r3 = 0** (finished).
7. If [STATE_SUBSTRUCT] >= 0 and previous checks passed to success:
   - Clear ONLINE_FLAG = 0.
   - Clear ADDR_15578 (`0x82BF3CDA`) = 0.
   - Call `sub_8223DB90()` — final cleanup.
   - Set STATE_VAR = **0** (reset).

**Transitions**:
- sub_822417B0 returns 2 -> return 2
- sub_822417B0 returns nonzero/non-0 -> wait (return 1)
- [STATE_SUBSTRUCT] < 0 -> state 13
- Various completion paths -> return 0 (finished)
- Full success with reset -> state 0 + return 1

---

## State 13 — Error Recovery / Retry

**Label**: `loc_822431DC`

**Calls**:
- `sub_8223DB20()` — abort check
- `sub_82240B78()` — validation
- `sub_8223F9F0(r3=4, r4=neg([STATE_SUBSTRUCT]), r5=&local_80)` — content re-enumeration
- `sub_8223DB90()` — cleanup

**Reads**: STATE_SUBSTRUCT (`0x82BF99C8`), ONLINE_FLAG

**Logic**:
1. Call `sub_8223DB20()`. If nonzero -> ERROR 33.
2. Call `sub_82240B78()`. If nonzero -> ERROR 34.
3. Read `[STATE_SUBSTRUCT]`, negate it (r4 = -value).
4. Call `sub_8223F9F0(4, negated_value, &local_80)`.
   - If returns zero -> wait (return 1).
5. Check `local_80`:
   - If zero: goto `loc_82242AB8` -> ERROR 6, return 2.
   - If nonzero:
     - Clear ONLINE_FLAG = 0.
     - Clear ADDR_15578 = 0.
     - Call `sub_8223DB90()`.
     - Set STATE_VAR = **0** (restart from beginning).

**Transitions**:
- Abort -> ERROR 33
- Validation fail -> ERROR 34
- No content -> ERROR 6
- Content found -> state 0 (full restart)
- Waiting -> return 1

---

## Summary: State Transition Graph

```
         +----> [0] Init
         |       |
         |       +---(ready)---> [4] Load Setup
         |       +---(not ready)-> [1] Wait Network
         |                          |
         |       +---(network)----> [3] Pre-Load
         |       |                  |
         |       +---- [2] Alt Content <---+
         |       |                         |
         |       +--------> [3] Pre-Load --+
         |                   |
         |                   +---(ready)---> [4] Load Setup
         |                                    |
         |                   +---(abort)--> ERROR 33
         |                   +---(mode err)--> ERROR 34
         |                   +---(ok)---> [5] Content Enum
         |                   +---(sub_82240B08)---> [9]
         |                                    |
         |            [5] Content Enum -------+
         |             |              |
         |        (content)     (no content)
         |             v              v
         |         [6] Create     [7] Retry
         |          w/Content      w/o Content
         |             |              |
         |        (success)      (content found)
         |             v              v
         |         [8] Monitor <------+
         |          Creation
         |             |
         |        (complete)
         |             v
         |         [9] Begin Load
         |             |
         |        (started)
         |             v
         |        [10] Monitor Load
         |             |
         |      (mode 0/1/3/4)    (other)
         |             v              v
         |        [11] Post-Load   DONE (r3=0)
         |             |
         |      (mode 3/4)        (other)
         |             v              v
         |        [12] MP Setup    DONE (r3=0)
         |             |
         |             v
         |        [14] MP Finalize
         |             |
         |      (success)  ([substruct]<0)
         |         v              v
         +---- [0] or DONE    [13] Error Recovery
                                  |
                                  v
         +---- [0] (restart) <----+
```

## Error Code Summary

| Code | Where Set | Meaning |
|------|-----------|---------|
| 6 | States 2, 7, 13 | Content not available / enumeration failed |
| 7 | State 6 | Scene creation call failed |
| 8 | State 8 | Scene creation error during progress check |
| 9 | State 8 | Scene creation unexpected result on completion |
| 14 | State 9 | Scene load initiation failed |
| 15 | State 10 | Unexpected load status during monitoring |
| 16 | State 10 | Unexpected load result on completion |
| 33 | States 4-11, 13 | Abort detected (sub_8223DB20 returned nonzero) |
| 34 | States 4, 9-11, 13 | Load/mode validation failed |

## Key Subroutine Summary

| Function | Purpose | Returns |
|----------|---------|---------|
| `sub_8223DAA0` | Readiness check | bool (u8) |
| `sub_8223DB20` | Abort/error check | bool (u8); nonzero = abort |
| `sub_8223DB90` | Final cleanup | void |
| `sub_8223CB60` | Content availability check | bool (u8) |
| `sub_8223CAD8` | Cleanup/teardown | void |
| `sub_8223D2F0` | Pre-load preparation | void |
| `sub_8223D400` | Post-load processing | bool (u8) |
| `sub_8223F308` | Structure initialization | void |
| `sub_8223F790` | Network session finalization | bool (u8) |
| `sub_8223F9F0` | Content enumeration | bool (u8); r5=out byte |
| `sub_826CBA70` | Network/session active check | bool (u8) |
| `sub_826CB6B8` | Network session teardown | void |
| `sub_82240AB0` | Pre-load setup | void |
| `sub_82240B08` | Secondary validation | bool (u8) |
| `sub_82240B78` | Load validation | bool (u8); nonzero = fail |
| `sub_822417B0` | Multiplayer scene setup/tick | int (0=done, 2=error, other=busy) |
| `sub_8284AAE0` | Create scene with content | bool (u8) |
| `sub_8284AB10` | Check scene creation started | bool (u8) |
| `sub_8284AB70` | Advance scene creation | void |
| `sub_8284ABA0` | Begin scene load | bool (u8) |
| `sub_8284ABD0` | Poll load status | bool (u8) |
| `sub_8284ABF8` | Advance load | void |
| `sub_8284B430` | Get creation/load result | int (0=ok, 2=retry, 5=ok-alt) |
| `sub_8284B490` | Get creation/load status | int (1=specific state) |
| `sub_8284B4B0` | Set scene name | void |
