<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Vulkan 1.1 + GLES 3.1 hardware baseline; eliminates devices that can't
         run the renderer at all from the install base. -->
    <uses-feature android:glEsVersion="0x00030001" android:required="true" />
    <uses-feature
        android:name="android.hardware.vulkan.level"
        android:version="1"
        android:required="true" />
    <uses-feature
        android:name="android.hardware.vulkan.version"
        android:version="0x400003"
        android:required="true" />

    <!-- Gamepad is not required but strongly recommended for playable input. -->
    <uses-feature
        android:name="android.hardware.gamepad"
        android:required="false" />
    <uses-feature
        android:name="android.software.leanback"
        android:required="false" />

    <!-- Storage access:
           - READ_EXTERNAL_STORAGE on API ≤32 for legacy side-loaded game files.
           - MANAGE_EXTERNAL_STORAGE is the modern equivalent and what the
             folder-picker flow actually uses (same pattern as reNut). -->
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE"
        tools:ignore="ScopedStorage" />

    <!-- Rumble, multiplayer, network state for pgStreamer/GNS. -->
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!-- Wake-lock: the GPU thread loop must not be suspended mid-frame. -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <application
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:theme="@style/AppTheme.Fullscreen"
        android:hasCode="true"
        android:allowBackup="false"
        android:largeHeap="true"
        android:hardwareAccelerated="true"
        android:requestLegacyExternalStorage="true"
        android:supportsRtl="true">

        <activity
            android:name="com.libertyrecomp.LibertySDLActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:alwaysRetainTaskState="true"
            android:screenOrientation="sensorLandscape"
            android:configChanges="keyboard|keyboardHidden|navigation|orientation|screenLayout|screenSize|smallestScreenSize|uiMode|layoutDirection|density|fontScale"
            android:preferMinimalPostProcessing="true"
            android:resizeableActivity="true">

            <!-- Expose the native library name the way SDL3's SDLActivity
                 expects it. This is ALSO overridden in code (getLibraries())
                 but manifest metadata is a useful fallback. -->
            <meta-data
                android:name="android.app.lib_name"
                android:value="LibertyRecomp" />

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
                <category android:name="android.intent.category.LEANBACK_LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
